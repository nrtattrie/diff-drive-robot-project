# Session 2026-07-22

## Current Phase
- Phase 1 — Foundation, Stage 2: Hardware Architecture + Validation + Chassis + Arduino.
- Stage 1 software simulation remains complete.
- Stage 2A selection-history audit, Q1, and Q2 are complete.
- Q2 now fixes only the owned `#4753` motors and 80 mm wheels/hubs as required
  Phase 2 reuse while plausible; all other Phase 2 properties and reuse choices
  are deferred.
- Q3 has been analyzed but remains unchecked. A specific driver recommendation
  is awaiting Nathan's approval; Q4 and later questions remain pending.

## What Was Accomplished This Session
- Restored context from the July 16 handoff, active architecture/phase documents, Git history, and working tree.
- Clarified that "build a preliminary model" means an auditable mathematical/computational wheeled-inverted-pendulum analysis, not physical assembly or chassis layout.
- Created `hardware/phase-2-dynamic-model.md` with:
  - evidence-state definitions for manufacturer, measured, assumed, derived, and bench-validated properties;
  - a physical-property register;
  - a planar two-wheel model including both wheel traction (`T/r`) and motor reaction torque (`-T`);
  - baseline and aggressive sensitivity cases;
  - limitations, measurements needed, provisional CAD accommodations, and a proposed component reuse boundary.
- Created `scripts/phase2_dynamic_model.py`, a standalone reproducible screening calculation with internal self-tests and equation-residual checks.
- Updated `hardware/stage-2-hardware-decision-record.md` with the Q2 findings and proposed reuse boundary.
- Updated `docs/phases.md` to link the Q2 analysis while deliberately leaving Q2 unchecked.
- Updated `README.md` and `docs/architecture.md` references.
- Corrected the Q2 scope after Nathan's review: it does not select or size the
  Q3 driver and does not decide reuse of any Phase 1 component except the
  motors and wheels/hubs.
- Re-evaluated current two-channel motor-driver products from manufacturer
  sources and updated `hardware/stage-2-hardware-decision-record.md`.
- Q3's high-level recommendation is the factory-assembled Pololu Motoron
  M2S18v18 `#5036`. Its useful headroom is the combination of configurable
  current limiting, independent command timeout, diagnostics, and telemetry;
  its 18 A rating alone is not the justification. The direct-PWM VNH5019
  `#2507` is the runner-up, and purchasing a second `#2991` remains rejected.
- At Nathan's request, moved the three new hardware documents from `docs/hardware/` into the existing top-level `hardware/` directory and repaired active links:
  - `hardware/component-selection-audit.md`
  - `hardware/stage-2-hardware-decision-record.md`
  - `hardware/phase-2-dynamic-model.md`
- Removed the now-empty `docs/hardware/` directory. The July 16 historical session record retains its original paths intentionally.

## Working Tree
- Branch `main` tracks `origin/main` with no reported ahead/behind divergence; HEAD remains `e98a792`.
- Modified, unstaged: `README.md`, `docs/architecture.md`, and `docs/phases.md`.
- Untracked before this save: `docs/sessions/session-2026-07-16.md`, the three Markdown files under `hardware/`, and `scripts/phase2_dynamic_model.py`.
- This session file remains untracked and was updated by the current
  save-session operation.
- No existing ROS or Arduino source files were modified.
- `docs/sessions/session-2026-07-16.md` was deliberately left unchanged as a historical record.
- No repository `AGENTS.md` exists; `find ..` only found unrelated files inside Codex/plugin data outside the repository.

## Verification
- `python3 -m py_compile scripts/phase2_dynamic_model.py` passed.
- `python3 scripts/phase2_dynamic_model.py --self-test` passed.
- Baseline 10-degree/0.30-second and aggressive 10-degree/0.15-second screening runs completed.
- The model self-test brackets Pololu's conflicting no-load-current figures, checks unit conversions and manufacturer points, verifies positive mass matrices, substitutes the nominal result back into both linear equations, and checks baseline/aggressive speed-headroom expectations.
- `git diff --check` passed after model creation and after the document move.
- Scoped local Markdown-link validation for the changed active documents passed after the move. An earlier repository-wide check reported only a URL-encoded BOM link and an unrelated ROS example link; neither was caused by this work.
- Manufacturer and model inputs were checked against primary Pololu, Bosch, MIT, University of Michigan, and published wheeled-inverted-pendulum sources; links are recorded in the model document.
- Q3 product status, pricing, motor capacity, Due logic compatibility,
  protection, assembly, and control-interface claims were checked against
  current official Pololu, Cytron, and Arduino documentation.
- The Q3 record was re-read after editing. No hardware or automated
  motor-interface test was possible; its I2C timing conclusion remains a paper
  calculation pending bench measurement.
- No hardware, ROS/Gazebo runtime, firmware, motor, IMU, timing, traction, thermal, or electrical bench tests were run. The Q2 results remain preliminary screening, not physical validation.

## Codex State
- The Q2 work and the four-step Q3 research/comparison/recording plan are
  complete.
- No active Codex goal exists.
- Q3 remains open because the phase directive requires Nathan's approval before
  marking the selection complete or promoting it to `docs/architecture.md`.
- Workspace-write sandbox is active for `/home/nate/diff-drive-robot-project` and `/tmp`; `.git` is readable. No escalation or approval is pending.
- The `save-session` skill restricted this operation to updating this one
  session file.

## Open Questions / Blockers
- Nathan must approve or revise the Q3 recommendation before the Q3 checkbox or
  canonical architecture is updated.
- The Motoron's main tradeoff is its 400 kHz I2C command path rather than direct
  PWM. Manufacturer documentation permits clock stretching and gives no
  worst-case command-to-output latency, so Stage 2B must test latency, jitter,
  electrical-noise behavior, and bus recovery.
- The Motoron has no meaningful over-temperature protection. Q4 must later
  define calibrated current limits, timeout duration, PWM behavior,
  brake/coast and fault behavior, stall detection, and thermal acceptance.
- Physical mass, center of mass, inertia, motor curves, backlash, IMU latency/jitter, encoder rate, traction, thermal behavior, and supply sag remain unmeasured.

## Context to Restore Next Session
- Nathan's confidence was rattled by the invalid assumptions uncovered previously. Continue separating manufacturer facts, assumptions, derived values, and bench validation; do not present a single reassuring number as proof.
- Nathan does not want replacement motors or wheels while the owned parts
  remain plausible. Q2 is intentionally independent of Q3, and unknown Phase 2
  properties must not be invented to justify present hardware.
- The Q2 model is a screening model, not a controller simulation or digital twin. It explicitly includes motor reaction torque, which a force-only cart-pole shortcut would omit.
- Baseline modeled current is approximately 0.15-0.44 A per motor. The analysis suggests wheel-speed headroom, control/sensor latency, unmeasured inertia, gearbox backlash, and traction become limiting before raw torque in the examined envelope.
- The aggressive tall/heavy 0.15-second recovery is speed-infeasible in the first-order model even though estimated current stays below 0.8 A.
- The model brackets Pololu's 0.080 A performance-curve and 0.20 A catalog no-load-current values rather than silently selecting one.
- The BNO055's manufacturer fusion-output rate is 100 Hz, but its actual end-to-end latency, jitter, conventions, noise, and Phase 2 suitability remain unverified.
- Q2 creates no evidence for a 17 A channel or for buying a second `#2991`.
- For Q3, "overengineered" should mean extra protection, observability, and
  credible future control capability—not raw amperage without motor-matched
  limits. The detailed candidate matrix and connection plan are already in
  `hardware/stage-2-hardware-decision-record.md`; do not duplicate them here.
- Nathan asked for the hardware documents to live in the existing root `hardware/` folder; keep them there.
- Do not check Q3, buy the recommended board, promote it into
  `docs/architecture.md`, or begin Q4 until Nathan responds.

## Next Action
- Wait for Nathan to approve or revise the Pololu Motoron M2S18v18 `#5036`
  recommendation for Q3.

## Last Response
- Codex recommended the Pololu Motoron M2S18v18 `#5036`, explained that its
  safeguards and telemetry—not merely its current rating—justify the Phase 1
  overengineering, retained the VNH5019 as the direct-PWM runner-up, and left
  Q3 open pending Nathan's approval.
