# Session 2026-06-29

## Current Phase
- Phase 1: Foundation.
- Stage 1: Software-Only is active and being rebuilt from scratch for learning.
- Current Stage 1 checklist state from `docs/phases.md`:
  - [x] URDF -- model robot geometry with chassis, wheels, caster; visualize via Foxglove.
  - [x] tf2 -- understand coordinate frames (`base_link -> odom -> map`).
  - [ ] Gazebo Harmonic simulation -- spawn URDF, drive with `teleop_twist_keyboard`.
  - [ ] `ros2_control + diff_drive_controller`.
- Gazebo is partially implemented and odometry worked for Nathan, but the latest headless Foxglove moving-robot path still needs Nathan verification before checking off the Gazebo box.

## What Was Accomplished This Session
- Restored context from prior session notes and inspected the inbox image after the AppArmor/bubblewrap issue was fixed.
- Corrected the URDF orientation and rough geometry:
  - wheels have the expected cylinder orientation and spin axis.
  - chassis was lifted so the wheel axle is 1 cm below the chassis underside.
  - drive wheels are front-biased for front wheel drive.
  - rear caster was added as a fixed visual/collision sphere tangent to the ground plane and chassis underside.
- Renamed the root link conventionally to `base_link` and discussed the purpose of the TF tree.
- Nathan checked off the URDF and tf2 Stage 1 boxes in `docs/phases.md`.
- Created and validated the Foxglove display helper script at `scripts/start_foxglove_display.sh`.
- Implemented the first Gazebo path without `ros2_control`:
  - added inertial properties and Gazebo's built-in `DiffDrive` plugin to `software/ros2_ws/src/diffbot_description/urdf/diffbot.urdf.xacro`.
  - added/updated `software/ros2_ws/src/diffbot_description/launch/gazebo.launch.py` to publish `robot_description`, start Gazebo, spawn `diffbot`, bridge topics, and start Foxglove support.
  - updated `software/ros2_ws/src/diffbot_description/package.xml` and `CMakeLists.txt` dependencies/install rules.
  - updated `docs/gazebo_sim_quickstart.md` with the current multi-terminal workflow.
- Debugged the missing odometry issue:
  - the GUI launch attempt exited cleanly over SSH, leaving only bridge/publisher processes.
  - default server-only Gazebo launch is the correct path for headless use.
  - Nathan later reported odometry was working.
- Added headless Foxglove moving-model support:
  - Gazebo joint states are published from the URDF plugin.
  - `/world/empty/model/diffbot/joint_state` is bridged to ROS `/joint_states`.
  - new script `software/ros2_ws/src/diffbot_description/scripts/odom_to_tf.py` publishes `odom -> base_link` TF from `/model/diffbot/odometry`.
  - `gazebo.launch.py` now starts `foxglove_bridge`.
- Latest build after the Foxglove/Gazebo launch changes passed with `colcon build --packages-select diffbot_description`.

## Open Questions / Blockers
- Nathan needs to restart the latest launch and verify Foxglove shows the robot moving through 3D space with fixed frame `odom`.
- The latest `odom_to_tf.py`, joint-state bridge, and built-in Foxglove bridge launch were build-tested but not yet live-tested by Nathan.
- Gazebo GUI is not expected to pop up cleanly over SSH; use the default server-only launch unless working directly on the Pi desktop.
- There is a nonfatal KDL warning risk because `base_link` has inertia. If it becomes annoying later, add a massless dummy root link or `base_footprint`.
- `ros2_control + diff_drive_controller` remains intentionally pending. The old generated `start_sim.sh`, `twist_relay.py`, and controller config are deleted in the working tree and should not be restored blindly.
- `CLAUDE.md` is not present. Restore the learning-first workflow from this session file and the recent session history.

## Context to Restore Next Session
- Nathan wants line numbers whenever referencing fixes or code locations.
- Learning-first remains important: explain concepts, keep steps small, and avoid large unexplained rewrites.
- Coordinate convention in use:
  - `+x` is forward.
  - `+y` is left.
  - `+z` is up.
- Static URDF/Foxglove display can use fixed frame `base_link`.
- Gazebo motion visualization in Foxglove should use fixed frame `odom`.
- Current Gazebo/Foxglove workflow:
  - Terminal 1:
    ```bash
    cd ~/diff-drive-robot-project/software/ros2_ws
    colcon build --packages-select diffbot_description
    source install/setup.bash
    ros2 launch diffbot_description gazebo.launch.py
    ```
  - Foxglove on the Mac:
    ```text
    ws://100.105.3.35:8765
    ```
    Set the 3D panel fixed frame to `odom` and enable Robot Model/TF.
  - Terminal 2:
    ```bash
    cd ~/diff-drive-robot-project/software/ros2_ws
    source install/setup.bash
    ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args -r /cmd_vel:=/model/diffbot/cmd_vel
    ```
- Expected result after pressing `i` in teleop: `/model/diffbot/odometry` changes, `odom -> base_link` TF updates, and the robot model moves in Foxglove without needing the Gazebo GUI.
- If odometry is missing, first check that the Gazebo process is still alive and that the default server-only launch is being used.
- If Foxglove connection fails, check for an old bridge process already using port `8765`.

## Next Action
- Restart the latest Gazebo launch, connect Foxglove to `ws://100.105.3.35:8765`, set fixed frame `odom`, drive with teleop, and confirm the robot model moves in Foxglove. If it works, decide whether the Gazebo Stage 1 box can be checked off or whether Nathan wants more explanation/testing first.

## Last Response
- Codex told Nathan the headless Foxglove moving-robot path had been wired in:
  - `gazebo.launch.py` bridges joint states, `/cmd_vel`, and odometry.
  - `gazebo.launch.py` starts `odom_to_tf.py` and `foxglove_bridge`.
  - `odom_to_tf.py` converts `/model/diffbot/odometry` into a moving `odom -> base_link` TF.
  - the URDF publishes Gazebo joint states for the wheel transforms.
- Codex asked Nathan to restart the launch, connect Foxglove to `ws://100.105.3.35:8765`, set fixed frame `odom`, run teleop remapped to `/model/diffbot/cmd_vel`, and verify that pressing `i` moves the robot in Foxglove.
