# Session 2026-06-24

## Current Phase
- Phase 1: Foundation.
- Stage 1 is active and being rebuilt for learning. `docs/phases.md` says Stage 1 is in progress and all four Stage 1 checklist items remain unchecked:
  - [ ] URDF — model robot geometry, visualize via Foxglove
  - [ ] tf2 — understand coordinate frames (`base_link -> odom -> map`)
  - [ ] Gazebo Harmonic simulation
  - [ ] ros2_control + diff_drive_controller
- Stage 0 hardware procurement is mostly complete, with LiDAR and battery deferred to Stage 4.
- Stage 2 has not started.

## What Was Accomplished This Session
- Re-established the project collaboration rule: this is learning-first. Prefer explanation, small steps, Nathan attempting implementation, and review/debugging over speed or generated full files.
- Reviewed the current rebuilt URDF and launch file in `software/ros2_ws/src/diffbot_description`.
- Confirmed `diffbot.urdf.xacro` expands successfully with `xacro`.
- Nathan moved `pkg_share` and `robot_description` setup inside `generate_launch_description()` in `display.launch.py`.
- Built and launched `diffbot_description` successfully:
  - `colcon build --packages-select diffbot_description`
  - `source install/setup.bash`
  - `ros2 launch diffbot_description display.launch.py`
- Verified ROS topics from the launch:
  - `/robot_description`
  - `/joint_states`
  - `/tf`
  - `/tf_static`
- Verified `joint_state_publisher` is publishing both wheel joints:
  - `chassis_to_left_wheel`
  - `chassis_to_right_wheel`
- Started Foxglove bridge successfully with `ros2 run foxglove_bridge foxglove_bridge`.
- Connected Foxglove Studio from Mac to Pi over Tailscale at `ws://100.105.3.35:8765`.
- Confirmed Foxglove visualizes the current simple robot: white chassis box, two black wheels, and frames for `chassis`, `left_wheel`, and `right_wheel`.
- Noted current local file state includes user/Nathan rebuild changes to:
  - `software/ros2_ws/src/diffbot_description/launch/display.launch.py`
  - `software/ros2_ws/src/diffbot_description/urdf/diffbot.urdf.xacro`
- No project code was edited by Codex during this save-session step.

## Open Questions / Blockers
- URDF geometry is intentionally rough and needs iteration.
- Current robot appears like a tall block/on its back because the chassis is `0.1 0.2 0.2` meters, so height is large relative to length.
- Wheels are mounted on the wrong-looking face/position for a normal flat diff-drive base.
- Need to decide later whether to keep `chassis` as the root link temporarily or introduce/rename to the conventional `base_link`.
- Local Codex sandbox/bubblewrap issue persists:
  - `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`
  - Normal sandboxed shell and local image viewer fail; escalated read commands have been used as a workaround.

## Context to Restore Next Session
- Learning-first rule is mandatory: explain concepts simply, use less technical language, ask Nathan to make small edits himself, and review/debug rather than writing full files by default.
- Before file edits, ask for explicit approval.
- Current launch/visualization feedback loop works:
  1. edit `software/ros2_ws/src/diffbot_description/urdf/diffbot.urdf.xacro`
  2. run `colcon build --packages-select diffbot_description`
  3. run `source install/setup.bash`
  4. run `ros2 launch diffbot_description display.launch.py`
  5. run `ros2 run foxglove_bridge foxglove_bridge`
  6. inspect in Foxglove via `ws://100.105.3.35:8765`
- In Foxglove, use fixed frame `chassis` for the current URDF.
- Simple coordinate explanation used this session:
  - `x` = front/back length
  - `y` = left/right width
  - `z` = up/down height
- Current likely URDF learning fix:
  - make chassis flatter, e.g. `box size="0.30 0.20 0.05"`
  - set chassis visual/collision origin to `xyz="0 0 0.025"`
  - move wheel joint origins near `xyz="0 0.105 0.04"` and `xyz="0 -0.105 0.04"`

## Next Action
- Have Nathan make one small URDF edit himself: flatten the chassis dimensions and adjust the chassis origin, then rebuild/relaunch and inspect the visual change in Foxglove before moving the wheel positions.
