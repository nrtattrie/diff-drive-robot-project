# Session 2026-03-13

## Current Phase

Phase 1, Stage 1 — all Stage 1 checklist items still unchecked (no project work done this session).

## What Was Accomplished This Session

- Continued SSH troubleshooting (entirely out-of-scope for project but blocking all progress)
- Updated `docs/troubleshooting/ssh-mac-to-pi.md` with full 2026-03-13 findings
- Re-enabled DHCP reservation in Deco for Pi MAC `2c:cf:67:dc:5b:d8` → 192.168.68.60 (had been disabled during prior troubleshooting)
- Deleted stale "nate-RasPi5" ghost entry at .55 from Deco device list
- Confirmed Pi SSH is running and Pi is healthy (monitor/keyboard)

## Open Questions / Blockers

- **SSH from Mac to Pi still broken** — this is the primary blocker
- Root cause is confirmed to be Mac-side: Mac kernel never sends ARP for .60 onto the wire
- macOS pf firewall rules (possibly left by VPN software) not yet checked
- Deco proxy ARP behavior not yet confirmed/ruled out
- Ethernet test (Pi plugged into Deco via cable) not yet tried

## Context to Restore Next Session

**Key confirmed facts:**
- Pi: .60, MAC `2c:cf:67:dc:5b:d8`, SSH running, UFW off, wlan0 UP
- Mac: .55, no VPN residual, no stale host route, no device isolation on Deco
- tcpdump on Pi confirmed: Mac NEVER sends ARP request for .60 onto the network — packet is killed in Mac kernel before hitting the wire
- Deco "Device Isolation" checked — Pi not listed
- `sudo route delete -host 192.168.68.60` returned "not in table"
- Pi IS visible and responding to ARP from other devices (seen in tcpdump)
- The "nate-RasPi5 at .55" in Deco was a stale cached entry — deleted

**Things tried this session that did NOT fix it:**
- `sudo arp -d 192.168.68.60`
- `sudo route delete -host 192.168.68.60`
- `sudo systemctl enable --now ssh` on Pi (SSH was already running)

**Suspected causes (in order):**
1. macOS pf rules from VPN not cleaned up
2. Deco doing proxy ARP for .60 and swallowing the response
3. Some other Mac routing table anomaly

## Next Action

From the Mac, run these three diagnostics:
```bash
sudo pfctl -sr 2>/dev/null | grep -E "60|block"
netstat -rn | grep "68\."
route get 192.168.68.60
```
If pf rules exist referencing .60 or broad block rules → flush pf: `sudo pfctl -F rules`
If clean → try ethernet test (Pi via ethernet cable into Deco).

## Last Response

Had just asked Nathan to run three Mac diagnostics (`pfctl`, `netstat -rn`, `route get`) to check for pf firewall rules or routing anomalies, after tcpdump confirmed the ARP request never leaves the Mac. Nathan said I was being cyclical and wanted to clear context — hence this save.
