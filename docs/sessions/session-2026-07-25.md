# Session 2026-07-25

## Current Phase

- `docs/phases.md` says Phase 1 Stage 2B is in progress.
- Stage 2A Q1-Q11 and Stage 2B gates B1-B2 are complete.
- B3, Due toolchain and safe-boot validation, is the next unchecked gate; B3-B28 remain pending.
- Nathan explicitly set the phase-order restriction aside for this session so the electrical connections and WireViz diagrams could be completed first. This wiring-documentation detour is complete.

## What Was Accomplished This Session

- Installed WireViz locally under `venv/wireviz` with `scripts/wireviz.sh`, organized diagrams by subject, and generated HTML, PNG, SVG, and TSV BOM outputs.
- Created or completed the following WireViz subjects:
  - `hardware/wiring/power/main-power-path.yml`
  - `hardware/wiring/motor-encoder/motor-wire-groups.yml`
  - `hardware/wiring/control-logic/control-logic.yml`
  - `hardware/wiring/control-logic/due-motor-driver.yml`
  - `hardware/wiring/control-logic/encoder-translator.yml`
  - `hardware/wiring/pi/pi-wiring.yml`
- Ensured the motor and encoder are represented as each motor's one physical six-wire factory group rather than unrelated assemblies.
- Added complete Due, `#2507` driver, and `#735` translator wiring, including fixed translator straps, all encoder channels, PWM/direction, fault, current-sense, and common-logic connections.
- Reconciled the BOM wiring/connectors with the diagrams:
  - Owned 16 AWG wire covers the battery trunk, fused segment, driver branch, and DROK input branch.
  - The owned waterproof holder and one 10 A fuse are assigned to F1.
  - Two ALITOVE pieces are shown as separate positive and negative three-way junctions, conditional on continuity proving the selected ports are common.
  - One owned 23 mm 2x20 extender exposes Pi pins 2/4/6/14 through the M.2 HAT+, with four short Elegoo/Dupont power leads.
  - The factory motor leads, `#2507` supplied screw terminals/breakaway header, Mega Prototype Shield V3, `#2204` socket, `#735`, and `104` capacitor are assigned.
  - Due-to-driver conductors are correctly identified as twelve male-to-female Dupont leads, male at the Due and female at the driver's male header.
  - Five-pin JST-SH, STEMMA QT, and Weewooday strips remain spare because forcing them into the baseline would require unnecessary adapters or create an unsuitable mobile-current path.
- Added the owner-reported Adafruit `#2998` hub, which BOM REV 02 omits:
  - One black Pi USB 2.0 port feeds the bus-powered hub.
  - Hub port 1 feeds `#954` and the BNO055; the other three hub ports are spare.
  - The Due stays direct on the other black Pi USB 2.0 port.
  - Future RPLidar stays direct on a blue Pi USB 3.0 port.
  - The hub's 1.35 mm external-power jack remains unused because it is directly tied to USB 5 V.
- Added the previously implicit Pi Active Cooler, M.2 HAT+/PCIe FFC, NVMe edge connector, and camera FFC as complete managed assemblies.
- Synchronized `README.md`, `docs/architecture.md`, `docs/assembly.md`, `hardware/power-signal-interface-matrix.md`, `hardware/stage-2-hardware-decision-record.md`, `hardware/stage-2-validation-log.md`, `hardware/wiring/README.md`, and `hardware/wiring/coverage.md`.

## Working Tree

- The tree is intentionally dirty and contains substantial earlier user/session work.
- Tracked modified files include `README.md`, `docs/architecture.md`, `docs/assembly.md`, and `docs/phases.md`.
- `hardware/Bill of Materials - Sheet1.pdf` is deleted while `hardware/Bill of Materials - Sheet REV 02.pdf` and `hardware/_archive/` are untracked; this replacement/archive state predates the final wiring pass and was preserved.
- Important untracked work includes the hardware decision/matrix/validation documents, the complete `hardware/wiring/` tree, `scripts/wireviz.sh`, dynamic-model files, and earlier session summaries.
- No files were staged or committed.
- `docs/phases.md` and unrelated prior changes were inspected but not modified during the final BOM/wiring reconciliation.

## Verification

- Extracted and reviewed BOM REV 02 using `pdftotext`.
- Inspected connector fit and current-path suitability; three read-only subagents independently audited BOM disposition, connector mating, and the hub topology.
- Checked Adafruit's official `#2998` product information and Raspberry Pi's official USB-current/single-TT guidance.
- Rendered all six main WireViz YAML sources successfully with WireViz 0.4.1 after the final edits.
- An initial power render failed because WireViz loops accept exactly two pins; the ALITOVE common junctions were corrected to chained two-pin numeric loops and rerendered without warnings.
- Visually inspected the main power, Pi, and complete control PNGs.
- Confirmed expected owned-part text appears in generated HTML/BOM output.
- `git diff --check` passed.
- A zero-size artifact search returned no files.
- No electrical continuity, polarity, voltage-drop, temperature, USB-enumeration, or powered hardware tests were performed; those require Nathan's physical hardware.

## Codex State

- The four-step wiring/BOM plan is fully complete.
- No active Codex goal exists.
- Workspace was `/home/nate/diff-drive-robot-project`, with write access limited to the repository and `/tmp`; no escalation was needed.
- WireViz is installed only in the repository-local ignored virtual environment, leaving system Python unchanged.
- No `AGENTS.md` exists in the repository.
- The generated diagrams are intentionally redundant where that makes a physical connection clearer. The complete views are very wide, so HTML/SVG zoom is the most usable format.

## Open Questions / Blockers

- Test whether the ALITOVE units provide a three-port common node or independent feed-through paths. Buy two known-common three-conductor lever connectors only if they fail this check.
- Inspect the actual 16 AWG colors and remaining length, holder/fuse type, terminal fit, and final installed lengths.
- Verify the 23 mm GPIO extension fits the actual M.2 HAT+ stack and confirm the required jumper genders.
- Confirm at least twelve suitable male-to-female Due/driver jumpers, four suitable Pi-power leads, one female-to-female BNO mode strap, and a data-capable USB-A-to-micro-B Due cable are on hand.
- Inspect the Mega Prototype Shield V3 rail map; use a small solderable perfboard only if it is unsuitable.
- Verify each motor housing's cavity order, leave the two motor-lead carrier-header positions unconnected, insulate abandoned stubs, confirm the 20 cm reach, and establish final polarity with an unloaded direction test.
- Measure the owned `#2998` captive lead, label the chosen downstream socket, and validate hub/`#954` enumeration and switch recovery.
- Before future LiDAR, validate the combined Pi USB/fan budget under the approved 5 A source configuration.
- Deferred purchases remain the 3S battery, compatible charger, matching prewired keyed connector, battery-specific undervoltage solution, and future RPLidar A1 development kit.

## Context to Restore Next Session

- Nathan wants a practical hobby build, not product-level overengineering. Prefer direct factory leads, supplied headers/terminals, simple soldered carrier wiring, and existing jumpers; do not add adapters merely to consume owned parts.
- Keep all JST-SH, STEMMA QT, Dupont, and terminal-strip wiring out of battery and motor-current paths.
- The selected USB layout is fixed unless physical testing disproves it: direct Pi-to-Due, `#2998` only for `#954`/BNO055, direct future Pi-to-RPLidar, and no external power on the hub.
- Pi USB-C bench power and DROK GPIO-header power are mutually exclusive.
- The ALITOVE symbols in the power diagram represent the intended common-positive and common-negative junctions, not proof of the actual product's internal topology.
- Useful entry points are `hardware/wiring/README.md`, `hardware/wiring/coverage.md`, and the generated HTML diagrams beside each YAML source.

## Next Action

- Resume the canonical build sequence with B3: install/confirm the Arduino Due toolchain, compile and upload the safe firmware skeleton, and verify reset-safe outputs, serial operation, and 5 V/3.3 V rails with motor power absent.

## Last Response

- Codex reported that every wiring diagram was updated and regenerated, summarized the owned-part assignments, and listed the remaining physical checks and conditional purchases.
- The response emphasized that no immediate generic connector purchase is justified: battery/LiDAR items remain later purchases, while lever connectors, USB cable, jumpers, perfboard, heavier Pi leads, or extra 16 AWG should be bought only if inspection or testing shows they are needed.
